{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UzytkownikOcenaGra list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Iduzocgr</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('uzytkownikocenagra_show', { 'id': entity.iduzocgr }) }}">{{ entity.iduzocgr }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('uzytkownikocenagra_show', { 'id': entity.iduzocgr }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('uzytkownikocenagra_edit', { 'id': entity.iduzocgr }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('uzytkownikocenagra_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
