{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Deweloper list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Iddeweloper</th>
                <th>Nazwa</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('deweloper_show', { 'id': entity.iddeweloper }) }}">{{ entity.iddeweloper }}</a></td>
                <td>{{ entity.nazwa }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('deweloper_show', { 'id': entity.iddeweloper }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('deweloper_edit', { 'id': entity.iddeweloper }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('deweloper_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
