{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gatunek list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idgatunek</th>
                <th>Nazwa</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gatunek_show', { 'id': entity.idgatunek }) }}">{{ entity.idgatunek }}</a></td>
                <td>{{ entity.nazwa }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gatunek_show', { 'id': entity.idgatunek }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gatunek_edit', { 'id': entity.idgatunek }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gatunek_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
