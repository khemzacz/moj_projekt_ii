{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Film list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idfilm</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('film_show', { 'id': entity.idfilm }) }}">{{ entity.idfilm }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('film_show', { 'id': entity.idfilm }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('film_edit', { 'id': entity.idfilm }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('film_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
