{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Autor</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idautor</th>
                <td>{{ entity.idautor }}</td>
            </tr>
            <tr>
                <th>Imie</th>
                <td>{{ entity.imie }}</td>
            </tr>
            <tr>
                <th>Nazwisko</th>
                <td>{{ entity.nazwisko }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('autor') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('autor_edit', { 'id': entity.idautor }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
