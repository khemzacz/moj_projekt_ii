{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Uzytkownik</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Iduzytkownik</th>
                <td>{{ entity.iduzytkownik }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ entity.login }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ entity.password }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('uzytkownik') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('uzytkownik_edit', { 'id': entity.iduzytkownik }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
