{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Uzytkownik list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Iduzytkownik</th>
                <th>Login</th>
                <th>Password</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('uzytkownik_show', { 'id': entity.iduzytkownik }) }}">{{ entity.iduzytkownik }}</a></td>
                <td>{{ entity.login }}</td>
                <td>{{ entity.password }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('uzytkownik_show', { 'id': entity.iduzytkownik }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('uzytkownik_edit', { 'id': entity.iduzytkownik }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('uzytkownik_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
