{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FilmRezyser</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idfilmrezyser</th>
                <td>{{ entity.idfilmrezyser }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('filmrezyser') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('filmrezyser_edit', { 'id': entity.idfilmrezyser }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
