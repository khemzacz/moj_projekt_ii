{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FilmRezyser list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idfilmrezyser</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('filmrezyser_show', { 'id': entity.idfilmrezyser }) }}">{{ entity.idfilmrezyser }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('filmrezyser_show', { 'id': entity.idfilmrezyser }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('filmrezyser_edit', { 'id': entity.idfilmrezyser }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('filmrezyser_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
