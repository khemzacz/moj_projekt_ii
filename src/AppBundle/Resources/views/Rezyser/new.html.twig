{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Rezyser creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('rezyser') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
