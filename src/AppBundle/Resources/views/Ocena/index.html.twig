{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ocena list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idocena</th>
                <th>Ocena</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ocena_show', { 'id': entity.idocena }) }}">{{ entity.idocena }}</a></td>
                <td>{{ entity.ocena }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ocena_show', { 'id': entity.idocena }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ocena_edit', { 'id': entity.idocena }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ocena_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
