{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ocena</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idocena</th>
                <td>{{ entity.idocena }}</td>
            </tr>
            <tr>
                <th>Ocena</th>
                <td>{{ entity.ocena }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ocena') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ocena_edit', { 'id': entity.idocena }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
