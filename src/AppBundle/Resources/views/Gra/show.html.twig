{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gra</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idgra</th>
                <td>{{ entity.idgra }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('gra') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('gra_edit', { 'id': entity.idgra }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
