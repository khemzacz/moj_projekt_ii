{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Producent list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idproducent</th>
                <th>Nazwa</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('producent_show', { 'id': entity.idproducent }) }}">{{ entity.idproducent }}</a></td>
                <td>{{ entity.nazwa }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('producent_show', { 'id': entity.idproducent }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('producent_edit', { 'id': entity.idproducent }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('producent_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
